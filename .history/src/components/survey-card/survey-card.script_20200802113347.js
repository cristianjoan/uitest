import { mapActions, mapGetters } from 'vuex'

export default {
    data () {
        return {
           
        }
    },
    props: {
        id: Number,
        name: String,
        category: String,
        argument: String,
        publishDate: String
    },
    methods: {

    }
}